<can-component tag="plan-ez-loader" leak scope>
  <style type="less">
    display: block;

    p { font-weight: bold; }
  </style>
  <view>
  {{#eq state "resolved"}}
    <content></content>
  {{else}}
    <div class="loading"></div>
  {{/eq}}
  </view>
  <script type="view-model">
    import DefineMap from 'can-define/map/';

    export default DefineMap.extend({
      message: {
        value: 'This is the plan-ez-loader component'
      }
    });
  </script>
</can-component>
